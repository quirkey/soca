# any pre build scripting should go here. As of v0.3 plugins are defined in config.json

# The context here is the Soca::Pusher so you have access to things like the `env` and `document`
# It's also already chdir'd into the application dir so you can manipulate the document
# or the local files
